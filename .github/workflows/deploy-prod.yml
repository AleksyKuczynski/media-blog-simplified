name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build image
        working-directory: ./frontend
        run: |
          docker build --no-cache \
            --build-arg NEXT_PUBLIC_DIRECTUS_URL=https://cms.event4me.vip \
            --build-arg NEXT_PUBLIC_SITE_URL=https://event4me.vip \
            --build-arg NEXT_PUBLIC_GA_MEASUREMENT_ID=${{ secrets.GA_MEASUREMENT_ID }} \
            --build-arg NEXT_PUBLIC_YANDEX_METRIKA_ID=${{ secrets.YANDEX_METRIKA_ID }} \
            -t event4me-prod:latest .

      - name: Restart container
        run: |
          docker compose -f /opt/event4me/prod/docker-compose.yml --env-file /opt/event4me/prod/.env up -d --force-recreate